// Intentionally empty. The game reaches this plugin through Capacitor's
// injected bridge (window.Capacitor.Plugins.AudioSession) like every other
// plugin — see src/platform/native.js — so no JS wrapper is needed. This file
// exists only so the package has a valid `main`.
export {};
